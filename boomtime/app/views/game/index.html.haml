#game
  - @area.features.each do |feature|
    = render :partial => 'feature', :locals => {:feature => feature}

= drop_receiving_element 'game', :accept => ['item', 'feature'], :hoverclass => 'hover', :onDrop => "function(drag, drop, event) { feature_dropped(drag, drop); }"