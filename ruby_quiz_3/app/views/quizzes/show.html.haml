- @quiz = Quiz.last unless @quiz
%h2
  = @quiz.id
  = @quiz.title

%h3 Description
= Maruku.new(@quiz.description).to_html

- unless @quiz.summary.blank?
  %a{:name => 'summary'}
  %h3 Summary
  = Maruku.new(@quiz.summary).to_html