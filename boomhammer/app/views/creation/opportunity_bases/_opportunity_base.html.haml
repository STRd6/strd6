.opportunity_base
  = opportunity_base.name
  - opportunity_base.loots.each do |outcome|
    .image.left= image_for outcome
    .left
      = "or" unless outcome == opportunity_base.loots.last
  .clear