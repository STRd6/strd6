- @characters.each do |character|
  %h3= character
  = link_to "View", character
  = link_to "Activate", :action => 'activate', :id => character.id

.left
  = link_to 'New character', new_character_path
