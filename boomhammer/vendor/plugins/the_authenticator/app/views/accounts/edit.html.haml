- form_for :account, @account, :url => { :action => "update" } do |form|
  = form.error_messages
  %p
    = form.label :nickname
    = form.text_field :nickname, :class => :text

  %p
    = form.label :email
    = form.text_field :email, :class => :text
  
  .right
    = submit_tag
