%div[game]
  = game.name
  %br
  .players
    - game.players.each do |player|
      = player.name
  .characters
    - game.character_instances.each do |character|
      = character.name
      %br

  .cards
    - game.cards.each do |card|
      = render :partial => card
      %br