require File.dirname(__FILE__) + '/test_helper'

describe Manager do
  before(:each) do
    @manager = Manager.new
  end

  it "should add tasks" do
    #@manager.add_task(Task.new(:target_cell, [:target_cell], :get))
  end
end

